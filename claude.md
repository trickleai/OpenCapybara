# Capybara - Claude Code Dashboard

> Claude Code 的可视化桌面应用

## 项目愿景

打造 Claude Code 的最佳桌面体验，以 **对话驱动** 为核心，提供简洁易用的知识工作界面。

**核心理念**：
- **Claude Code 优先** - 以 Claude Code 为核心交互方式
- 知识工作者不应该适应工具，工具应该适应工作流
- 对话即工作区

## 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    Capybara App                         │
│              (面向知识工作者的 GUI)                       │
├─────────────────────────────────────────────────────────┤
│                   Fork Code-OSS                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  重构/替换的部分：                                  │  │
│  │  - 隐藏 Activity Bar、Sidebar、Status Bar         │  │
│  │  - Editor Area → 对话为主的工作区                  │  │
│  │  - Secondary Sidebar → Session Dashboard          │  │
│  │  - 移除: Terminal、Debug、Git 等开发者功能         │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │  保留/复用的部分：                                  │  │
│  │  - 文件系统、编辑器核心、扩展宿主                    │  │
│  │  - Claude Code VSCode 扩展 (内置)                  │  │
│  └───────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                   Electron Runtime                      │
└─────────────────────────────────────────────────────────┘
```

## UI 设计

### 当前布局

```
┌─────────────────────────────────────────────────────────────┐
│                    Title Bar (Command Center)               │
├─────────────────────────────────────────┬───────────────────┤
│                                         │                   │
│                                         │ Session Dashboard │
│              主工作区                    │                   │
│                                         │  ┌─────────────┐  │
│      Claude Code 对话面板                │  │  Sessions   │  │
│                                         │  │  会话列表    │  │
│      （对话是主要交互方式）               │  ├─────────────┤  │
│                                         │  │  Status     │  │
│                                         │  │  状态指示    │  │
│                                         │  └─────────────┘  │
│                                         │                   │
└─────────────────────────────────────────┴───────────────────┘
```

### UI 设计原则

1. **极简界面** - 隐藏所有开发者 UI（Activity Bar、Status Bar、Sidebar）
2. **对话即工作区** - Claude Code 对话面板是主工作区
3. **会话可视化** - 右侧 Dashboard 展示 Claude Code 会话状态
4. **用户友好** - 简洁直观的界面设计

---

## 开发计划

### Phase 1: 基础骨架（MVP）✅ 已完成

**目标**：可运行的独立 App，内置 Claude Code，基础品牌

- [x] Fork Code-OSS 并建立构建流程
- [x] 品牌资产（Capybara 名称、图标）
- [x] 基础配置：product.json 品牌替换
- [x] 内置 Claude Code 扩展
- [x] 创建默认配置扩展 (capybara-defaults)
- [x] 编译并成功运行

**产出**：可以启动的独立 App，能用 Claude Code 对话

### Phase 2: UI 简化 ✅ 已完成

**目标**：知识工作者友好的极简界面

- [x] 隐藏 Activity Bar
- [x] 隐藏 Status Bar
- [x] 隐藏 Primary Sidebar（启动时）
- [x] 隐藏开发者功能（SCM、Extensions、Debug、Testing）
- [x] Session Dashboard 放置在右侧（Secondary Sidebar）
- [x] 创建正式应用图标

**产出**：界面明显区别于 VSCode，更像知识工具

### Phase 3: 打包分发 🚧 进行中

**目标**：可分发的产品

- [x] Mac DMG 打包
- [ ] Windows Installer 打包
- [ ] 自动更新机制
- [ ] 代码签名

---

## 技术栈

| 组件 | 选择 | 说明 |
|------|------|------|
| 基座 | Code-OSS (MIT) | Fork 自 microsoft/vscode |
| 运行时 | Electron 39.2.7 | 随 Code-OSS |
| AI 引擎 | Claude Code 扩展 v2.0.75 | 官方扩展，预装 |
| Session 系统 | capybara-agent 扩展 | 会话追踪和 Dashboard |
| 构建 | gulp + webpack | Code-OSS 原生 |
| Node.js | v22.21.1 | 开发环境要求 |

---

## 项目结构

```
/Users/jarodxu/knowledge-app/
├── .build/
│   └── electron/
│       └── Capybara - Claude Code Dashboard.app  # 构建产物
├── extensions/
│   ├── claude-code/            # Claude Code 扩展 (预装)
│   ├── capybara-agent/         # 会话追踪扩展
│   │   ├── src/
│   │   │   ├── extension.ts              # 入口
│   │   │   ├── core/
│   │   │   │   └── ClaudeSessionTracker.ts  # 会话追踪
│   │   │   └── ui/
│   │   │       └── SessionDashboardProvider.ts  # Dashboard UI
│   │   └── package.json
│   └── capybara-defaults/      # 默认配置扩展
│       ├── src/extension.ts    # UI 隐藏逻辑
│       └── package.json        # 配置默认值
├── resources/
│   └── darwin/
│       └── capybara.icns       # 应用图标
├── src/                        # VSCode 源码
├── out/                        # 编译输出
├── product.json                # 产品品牌配置
├── package.json
└── CLAUDE.md                   # 本文档
```

---

## 开发命令

```bash
# 确保使用正确的 Node 版本
source ~/.nvm/nvm.sh && nvm use 22.21.1

# 编译项目
npm run gulp -- compile

# 编译扩展
cd extensions/capybara-defaults && npm run compile
cd extensions/capybara-agent && npm run compile

# 启动开发版本（推荐）
./start-capybara.sh

# 或使用官方脚本
bash scripts/code.sh

# 带详细日志启动（调试用）
./start-capybara.sh --verbose
```

---

## 品牌配置

当前配置（product.json）：

- **名称**: Capybara
- **全称**: Capybara - Claude Code Dashboard
- **应用名**: capybara
- **数据目录**: ~/.capybara
- **Bundle ID**: com.capybara.app
- **URL Protocol**: capybara://

---

## 注意事项

1. **Node.js 版本**：必须使用 v22.21.1，因为项目使用了 TypeScript 直接执行和 ESM 模块
2. **Native 模块**：需要针对 Electron 版本编译，使用 `npm rebuild` 重新编译
3. **扩展市场**：当前配置使用 Open VSX，可根据需要调整
4. **Claude Code 扩展**：从 VS Marketplace 下载，需要用户自行配置 API Key

---

## 参考资源

- [VSCode 源码](https://github.com/microsoft/vscode)
- [Claude Code 文档](https://docs.anthropic.com/en/docs/claude-code)
- [Electron 文档](https://www.electronjs.org/docs)
